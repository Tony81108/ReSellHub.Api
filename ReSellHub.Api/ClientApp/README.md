# ReSellHub Vue 前端

## 開發模式

```bash
npm install
npm run dev
```

Vue 開發網址：`http://localhost:5173`。請同時啟動 ASP.NET Core HTTPS 專案。

## 發布建置

```bash
npm run build
```

產出會放入 `wwwroot/app`，之後按 F5 可由 ASP.NET Core 直接開啟 `/app/index.html`。
